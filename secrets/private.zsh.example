# Copy this to: ~/.config/shell/private.zsh
# This file is sourced by zsh/.zshrc if present.

# Example: secret used by `totp` alias in zsh/.zshrc
# export TOTP_SECRET='replace_with_base32_seed'

# Example: only if you really need this workflow
# export SUDO_PASS='replace_me'
# alias sd='export SUDO_PASS; ~/bin/sd.sh'
